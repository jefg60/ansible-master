#!/bin/bash
cd {{ git_repo_target }} || exit
unset GIT_DIR
git fetch
git merge origin/{{ git_branch }}
git checkout {{ git_branch }}
cd {{ git_log_dir }} || exit
echo "$(date) {{ git_repo_name }} has been pushed" >> {{ git_log_dir }}/{{ git_repo_name }}.log
